apiVersion: apps/v1
kind: ReplicaSet
metadata:
  name: rs-gpu-exists
  namespace: gpu-test
spec:
  replicas: 2
  selector:
    matchLabels:
      app: prober-gpu-exists
  template:
    metadata:
      labels:
        app: prober-gpu-exists
    spec:
      imagePullSecrets:
        - name: dockerhub-secret
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - key: gpu
                    operator: Exists
      containers:
        - name: http-prober
          image: datatomas/uppercut_analytics:http-prober
          imagePullPolicy: Always
          livenessProbe:
            exec:
              command: ["sh","-c","python -c 'print(\"ok\")'"]
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 2
            failureThreshold: 10
